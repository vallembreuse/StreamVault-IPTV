name: Apply watched detail actions

on:
  push:
    branches:
      - feat/watch-progress-cards
    paths:
      - .github/workflows/watch-progress-detail-actions.yml

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ready
